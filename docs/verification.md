# 首版验收记录

## 按行为合并测试（2026-09-10）

后端从 337 项减为 **281 项**，减少 56 项（16.6%），测试代码从 7,130 行减为 6,714 行；前端保留 94 项。相近输入使用具名 `subTest` 共享准备与断言，重复的功能路径集中验证。测试发现规则不变，没有隐藏、跳过或挪出默认套件的用例。

| 合并范围 | 当前覆盖位置 |
| --- | --- |
| 所有变更接口的未登录、会话过期、缺请求头、跨域拒绝与数据不变 | `test_api.py` 的写接口矩阵；删除各功能重复的认证用例 |
| 改密、会话撤销、重启、Unicode 密钥与账本保留 | `test_admin_password.py` 的完整改密流程；失败回滚与并发仍独立验证 |
| 基金分类、HTTP 重试、行情回退、收盘补齐 | `test_data.py`、`test_source_priority.py`、`test_history_speed.py` 的参数表 |
| 缓存命中、增量、节假日、长缺口、周期刷新及无效增量 | `test_incremental_history.py` 的窗口和无效输入矩阵；两套复权事务回滚仍独立验证 |
| 分时缓存与只读接口、收益窗口、手动名单与分页排序 | `test_intraday.py`、`test_market_performance.py`、`test_manual_etfs.py`；删除基础 API 文件中的重复排序测试 |
| 通知不重放、禁用取消、队列事务回滚及去重 | `test_notifications.py` 的生命周期与批次测试；不确定送达、并发、脱敏仍独立验证 |

完整后端回归 281 项通过。保留交易账本、T+1、订单及行情新鲜度、历史因果性、数据库迁移与运行隔离的既有关键验证。

前端 94 项、Ruff、JavaScript 语法、Prettier、Vite 构建及 Compose 配置检查通过。已重建镜像并更新 dashboard，容器内文件与验证后的工作区一致；worker 未重启，两项服务均健康。本机与公网 `/healthz` 返回 200，公开数据接口与首页资源正常，未登录设置接口返回 401。

## 测试精简（2026-09-10）

第一轮精简后为后端 337 项、前端 94 项。后文的测试数量与功能描述均为对应版本的历史记录。

- 删除自动筛选时代的“同类代表排名不变”用例；手动名单已不选代表，保留 `test_manual_etfs.py` 对旧筛选条件不影响名单及同方向多只入选的验证。
- 删除重复的同指数入选用例；证券资格校验统一由 `test_strategy.py` 的多资产／市场用例覆盖，短历史和下降趋势继续单独验证。
- 将裸 K 禁止日频配置及配置不变断言集中到 `test_api.py`；公开账户、信号与未知路由测试移除旧登录前置条件，认证边界仍单独覆盖。
- 将无新增成交量与成交量重置合并至 `test_trading.py`；旧日成交量不匹配由 `test_incremental_history.py` 覆盖，删除 `test_history_speed.py` 中的重复用例。
- 删除换手率纯计算测试中无用的数据库、同类候选和筛选辅助函数；旧范围参数仅保留历史配置可读性验证，已移除参数的 API 拒绝行为仍由手动名单测试覆盖。

保留旧数据库迁移、账本核对、T+1、并发去重、权限、行情回退和历史因果性回归。验证入口仍为 `bash scripts/validate.sh`，未增加跳过项或缩小测试发现范围。

完整验证通过：Ruff、后端 337 项、前端 94 项、语法及格式检查、Vite 构建、Compose 配置和差异空白检查。重建镜像并仅更新 dashboard，worker 容器未重启，两项服务均健康。容器中的应用、配置、脚本、测试及前端产物与工作区哈希一致；本机与 `https://niualpha.com` 的 `/healthz` 均返回 200，首页资源一致，公开接口可读，设置接口未登录返回 401。


## 手动添加输入联想（2026-09-09）

后续交互验收：点击提示项或键盘确认直接添加，添加期间阻止重复提交，仍支持完整代码手动提交。前端 55 项测试、格式检查和构建通过；独立临时数据库中已验证点击 `SH588170` 提示后立即出现在列表、输入框清空并显示成功提示。

“添加 ETF”支持代码、名称及已有跟踪指数资料的匹配提示，显示名称、沪深代码和“已添加”，支持鼠标与 ↑↓／Enter 选择。点击提示项或用 ↑↓／Enter 确认后直接提交添加；搜索请求本身仍只读，不恢复全市场自动采集。已有资料不足时按关键词向东方财富查询，核验基金类型及交易所，排除股票、指数、LOF 和联接基金；空结果与查询失败分别显示，完整代码仍可直接提交核验。

相关后端 50 项、前端全部 53 项通过，Ruff、Prettier、生产构建与差异空白检查通过。测试覆盖搜索限额、名称／代码规范化、交易所身份、缓存、只读查询、延迟请求乱序、清空／选择／卸载取消、失败提示和代码提交校验。真实东方财富接口只读验证 `510300`、`沪深300`、`新能源`，返回可核验的 ETF 名称与代码。

独立临时环境实际验证中文名称、代码前缀、鼠标及键盘选择、选择不立即入池、添加及已添加标记；桌面与 390px 视口下拉列表正常，手机内容宽度 379px，控制台无错误或警告。测试名单与实际账户隔离。


## 手动 ETF 名单更新（2026-09-09）

全市场目录采集与自动关注池筛选已移除，默认名单为空；ETF 经代码手动添加，提供移除与重复添加幂等处理。旧目录归档，升级保留持仓、历史成交和账本，只继续采集手动名单及现有持仓／风险订单所需标的。后续章节属于历史版本的验收记录，其中旧全市场、流动性门槛和同类去重规则不再适用。

当前后端套件共 252 项：251 项通过，1 项既有失败 `test_credential_sessions_and_audit_roll_back_together`（测试期待原始 SQLite 异常，但现有 API 错误处理返回 503；账户回滚逻辑未被本次修改）。新名单测试覆盖入池不依赖分类、成交额或换手率、同方向多只、缺数据保留、归档排除、管理员及同源校验、错误代码／非 ETF／源失败、重复添加、移除保护、配置版本及重启、持仓与账本迁移、采集范围、过期异步结果、收盘报价展示与交易新鲜度。

前端 49 项测试、Ruff、Prettier、Vite 构建和差异空白检查通过。使用独立临时数据库实际验证登录、四个设置页签、完整策略参数表单、ETF 添加、重复提示、移除后空列表；桌面与 390px 手机布局正常（内容宽度 379px），控制台无错误或警告。界面测试使用注入的基金资料，无订单生成，不改变实际使用名单。

验收日期：2026-09-07，北京时间。范围为本地模拟交易软件及公开数据适配器，不以收益为验收门槛。

部署后核验：dashboard 与 worker 均健康，配置版本 16，手动名单 0 只，采集范围仅原有 3 只持仓。迁移前备份 `/data/backups/before-manual-etfs-20260909-182740.sqlite3`；持仓、成交、现金与份额账本指纹与备份一致，核对无错误。运行页面已显示“我的 ETF”和手动添加入口。

## 自动化验证

| 检查 | 结果 |
| --- | --- |
| Ruff 静态检查 | 通过，`app tests scripts` |
| Python 测试 | 54 项通过；宿主 Python 3.12.12 与 Docker Python 3.12 均通过 |
| 前端语法检查 | state、main、Vite 配置通过 |
| Vite 生产构建 | 通过，34 个模块；JS 131.37 kB，CSS 28.68 kB；Prettier 格式检查通过 |
| Compose 配置及镜像构建 | 通过；dashboard、worker 镜像使用锁定依赖 |
| 管理员边界 | 公开行情可读，账户未登录 401；同源、写请求头、密码限流和会话退出测试通过 |
| 在线备份及读取 | SQLite backup API 成功；只读打开备份并重建账户，核对一致 |

测试分布：数据 15 项、策略 7 项、交易账本 21 项、API 7 项、工作流 4 项。数据源 501 拦截测试证明一次失败后不继续请求同一源，历史调度进入冷却。

覆盖证券代码、三位价格、成交单位、未知分类、跨境及沪港深混合指数排除、基金身份、分页缺失、空目录保护、复权日期一致、备用源成交额、源熔断、休市日与未知日历、输入时间边界、动量排名、指数去重、持仓保留和仓位约束。

交易测试覆盖订单后报价、费用和滑点、按订单最低佣金、部分成交、源切换及同时间样本去重、T+1 跨中秋假期、现金和持仓额度、停牌／盘口未知／涨跌停、分红登记与卖出后到账、份额折算、不可核验余额、暂停／恢复、普通订单收盘失效、持久化风险退出、组合回撤仅统计且不拦截交易、无效估值不误触止损、重启和并发去重、不可修改账本及成本损坏阻断。

## 独立 Compose 两日流程

执行 `scripts/acceptance.sh`，使用 `niuno3-acceptance` 项目及独立卷，固定 2026-09-07 / 2026-09-08 行情。每个阶段新建一个容器，读取同一个测试数据库。

| 阶段 | 容器启动后的验证 | 成交总数 | 核对 |
| --- | --- | --- | --- |
| buy | 9 月 7 日冻结计划、提交订单、随后报价买入 | 1 | 一致 |
| hold | 新容器读取账本，同日可卖数量 0，重复撮合无新增成交 | 1 | 一致 |
| exit | 9 月 8 日新行情触发成本退出，已解锁份额卖出 | 2 | 一致 |
| verify | 再次启动并重试，持仓归零、现金与费用及损益对应 | 2 | 一致 |

四阶段全部 `ok=true`，核对错误为空；成功后自动清理该测试项目及卷。测试数据库与正式模拟账户隔离。

## 公开数据连通性

只读全量目录成功返回 **1,604** 只沪深 ETF；每页总数和重复证券校验通过。一次小规模 `python -m scripts.probe` 对 `sh510300` 的结果：基金类别识别通过、腾讯报价带来源及时间、最近完整日 2026-09-04、320 根历史日 K、15 条公司行动记录。

本机东方财富历史接口出现远端断开，备用价格／实际成交额通道曾成功完成读取；随后持续预热期间腾讯历史接口返回 HTTP 501 拦截页，已加入每主机限速及至少 5 分钟冷却。交付时全量历史仍未就绪，系统保持禁止新增买入并自动退避重试。早先的小规模成功不代表所有基金已预热或源持续可用。`readyz` 按设计返回 503，页面列出覆盖不足与冷却原因。

## 页面验收

通过内置浏览器实际操作并查看页面：

| 视口 | 五页面 | 页面横向溢出 |
| --- | --- | --- |
| 1280 × 900 | 总览、市场、信号、账户、设置 | 无，内容宽度 ≤ 1280 |
| 390 × 844 | 同上 | 无，内容宽度 375 |
| 320 × 740 | 同上 | 无，内容宽度 305 |

宽表格在自身滚动容器内横向滚动，不撑开手机页面。实际验证管理员登录、市场代码搜索、ETF 详情抽屉、暂停、恢复、路由返回页面顶部；缺数据、无持仓、历史预热及运行异常提示可辨识。布局验收时浏览器控制台无警告或错误。

## 交付时的边界

公开源限速、历史缺失及基金资料不明会影响就绪时间，失败不伪造成交。指数名称未建立别名映射，不同别名可能无法去重。包含不足一份余额的折算及旧公司行动修订会暂停标的核算，首版不提供强行覆盖账本的操作。日历覆盖至 2026-12-31，下一年需依据正式公告扩充。

未包含券商实盘接口、历史回测界面、大模型决策和外部推送，均符合首版约定范围。

## 同类型唯一代表更新验收

新增 `tests/test_focus.py` 的 10 项测试与 API 的 1 项测试，完整套件共 **65 项通过**。覆盖同方向不同指数合并、宽基名称规范化、不同资产／市场隔离、流动性第一未过趋势或 120 日门槛时不递补、缺数据整组等待、未满 20 日明确排除、并列及未来数据、低流动性旧持仓退出、撮合层同类持仓约束，以及规则升级保留历史和幂等迁移。

API 验证了先在完整同类范围内选代表，再搜索和分页；直接搜索第二名不会把它变成代表。新版本再次通过独立 Compose 两交易日与重启验收。前端构建和格式检查通过；市场页默认显示同类代表，并验证取消筛选可查看全量与关注原因。320px、390px 页面没有整页横向溢出。

分组与成交额数据不足的方向保持“待确认”，全市场目录和历史采集继续运行。分组规则和细节见 `docs/strategy.md`、`config/focus_groups.json`。

## 浅色圆角主题更新验收

对齐 NiuOne 标准浅色主题：灰白背景 `#f5f6f8`、白色卡片、蓝色强调 `#2457c5`、12px 卡片圆角、7px 表单控件圆角及轻阴影。侧栏、手机导航、策略选项卡、收益曲线、详情抽屉与登录页统一使用主题变量；涨跌、运行状态和异常提示保留独立语义颜色。

Vite 生产构建及 Prettier 检查通过。使用独立临时数据库和固定行情验证登录、持仓、策略与设置页面，在 1280px、390px、320px 视口逐页检查五个页面，均无整页横向溢出，浏览器控制台无警告或错误。本次仅调整前端主题，未更改交易和账本代码。

## 交易通知模块验收

新增飞书、钉钉、企业微信和 Telegram 通知，默认关闭，渠道配置与 NiuOne 独立。通知使用已提交成交分录、持久化游标和独立线程池；配置单独版本化，不改变交易配置或账户账本。

新增 `tests/test_notifications.py` 的 24 项测试，完整套件共 **89 项通过**，宿主与 Docker Python 环境均通过。覆盖四渠道确认回执、飞书／钉钉签名固定向量、官方地址校验、重定向拒绝、请求超时、日志及错误脱敏、配置保留／移除、版本冲突、管理员与同源边界、未保存配置的单渠道测试、成交回滚、部分成交合并、长消息拆分、队列事务回滚、重启去重、慢渠道及失败隔离、卖出损益和发送中断后的不确定状态。

独立 Compose 两交易日买入、持有、退出和重启验收再次通过。前端生产构建、Prettier、Ruff 与差异空白检查通过。浏览器实际操作验证渠道添加、测试成功／失败、保存后不回显密钥、关闭保留配置、移除保存后重新添加显示未设置。1280px、390px、320px 均无整页横向溢出，控制台无警告或错误。

所有通知请求验证均使用 HTTP 模拟传输或注入测试发送器，没有使用真实机器人凭据或向外部联系人发送消息。实际渠道的送达验证需管理员填写自己的配置并点击测试通知。说明见 `docs/notifications.md`。

## 历史日 K 下载进度验收

总览与设置页新增同一进度卡片，直接使用 `/api/v1/status` 的 `history_progress`。覆盖率、完成／待补充、最近 5 分钟成功数量、失败标的数量、覆盖门槛缺口、目标交易日与最近成功时间均来自 SQLite 记录；展示源冷却、自动重试、Worker 离线及空目录等状态。

新增 5 项进度测试，并扩展状态接口测试，完整套件 **94 项通过**。覆盖当前范围统计、短历史成功下载、失败去重、门槛向上取整、达到门槛但未全部完成、目标交易日切换、仅有成功记录但缺日 K、未知日历、冷却到期、心跳过期和暂停交易仍采集。修正了未知目标交易日时空历史可能被误计为已完成的问题。

Ruff、Prettier、Vite 生产构建和 Compose 配置检查通过；已更新 dashboard 与 worker，健康检查正常。部署后在真实设置页及总览验证进度展示和自动刷新；1280px、390px、320px 均无整页横向溢出，检查时浏览器控制台无警告或错误。页面验收发现并修正了读取进度期间心跳跨秒更新可能误显离线的问题，补充对应断言后 13 项进度与 API 测试再次通过。

## 历史下载队列修正验收

2026-09-07 16:09 检查发现，最近 15 分钟历史任务仅 3 只成功、433 次失败，涉及 148 个失败标的；原先按代码固定顺序、每 5 分钟重试的调度让后续标的长期得不到处理。实时抽查 `sh510300` 与 `sh561360`，腾讯价格最后日期为 9 月 7 日，同花顺年度数据最后日期仍为 9 月 4 日，说明收盘后跨来源更新不同步也是当前完成率低的原因。旧历史仍保留，页面完成率在 15:30 后以当日为目标重新统计。

历史任务改为优先级内公平轮转，失败采用持久化递增退避，保留原有并发和数据校验。新增 `tests/test_history_queue.py` 的 6 项测试，覆盖失败标的不饿死后续队列、重试间隔与上限、重启保留退避、在途重启、目标日期切换、持仓优先与并发上限、成功清理状态及陈旧响应保护。完整 **100 项测试通过**，Ruff、Prettier、前端生产构建、Compose 配置和差异检查通过。上游当天数据未发布时仍不会计为完成，未降低覆盖率或成交质量门槛。

## 历史增量更新验收

新增增量窗口规划、5 日重叠校验、缓存指纹检查和前复权／未复权事务更新。正常缺一天时价格请求上限 8 根，仅写 1 根新增与 5 根已核验重叠日 K；其他保留行的来源与采集时间不改变，历史维持最多 320 根。基准变化、源切换、缺失范围过大等情形回退完整刷新，并每 30 个自然日完整核验一次。旧数据不足以完成当前目标时保留缓存并重试。

新增 `tests/test_incremental_history.py` 的 14 项测试，并扩展进度测试。完整 **114 项测试在宿主和 Docker 中通过**，覆盖单日追加、同日零请求、复权变更、源切换、缺重叠、缺中间日／最新日、异常数据、完整刷新失败、首次／短历史／长缺口／周期核验、休市日、320 根裁剪、旧证据保留、并发陈旧结果拒绝、重启缓存复用、两套复权数据共同回滚，以及两种适配器的增量请求范围与成交量校验。独立 Compose 两交易日买入、持有、退出、重启和核对流程通过。

公开源只读抽查使用 `sh510300` 已保存的截至 9 月 3 日历史副本，补到已完成的 9 月 4 日，实际走增量路径：319 根变为 320 根，两次腾讯价格请求（各请求 8 根）加一次同花顺近期窗口，总响应约 15 KB、耗时 1.23 秒。该结果是单标的连通性验证，不能推算全市场就绪时间；没有向运行数据库写入探测数据。当天上游尚未发布的日 K 继续保持未完成状态。

Ruff、Prettier、前端构建和 Compose 检查通过。dashboard 与 worker 已更新，账本核对无异常；页面新增已缓存历史与本轮增量完成数量，部署时自动识别原有缓存。设置页在 1280px、390px、320px 无横向溢出，新增说明可读，控制台无警告或错误。

## 历史获取速度与收盘补齐优化验收

2026-09-07 17:24 的运行检查显示：境内股票 ETF 当日历史完成 195/1211；最近 15 分钟 418 次失败涉及 418 个不同标的，其中 188 次为价格／金额日期不一致、152 次成交量交叉校验失败。队列已在公平轮转，当前主要问题是公开源历史文件缺当天或保留盘中样本，以及 HTTP 502 等上游失败。

成交额请求改成与价格相同的有界 `lastN.js` 窗口；先检查易失败的成交额通道；历史主源故障采用单个探测，陈旧主源立即切备用源，400／404 不做同次立即重试。新增只在收盘时间、身份、OHLC 和成交份额均核验通过时使用的 `today.js` 补齐通道，修正腾讯末根前复权价格临时只返回两位小数的情况。普通重启保留失败退避，下载实现升级允许旧失败任务按正常并发重试一次。

新增 13 项数据获取测试及 1 项升级队列测试，完整 **128 项测试在宿主和 Docker 中通过**。覆盖末根缺失／盘中金额、当天空字段占位行必须经独立收盘校验才补齐、三位小数、完整归档无需额外请求、收盘状态和日期、未来时间、代码身份、价格不符、份额差异不足 2% 仍拒绝收盘补齐、非法金额、旧日量异常、成交额故障不请求价格、有界完整窗口、陈旧主源切换、并发故障探测、404 单次尝试，以及升级后退避和重启。Ruff、Prettier、前端构建、Compose 配置及差异检查通过；隔离 Compose 的买入、持有、退出、重启和账本核对全部通过。

同一时段只读对比（复用截至 9 月 4 日缓存副本，目标 9 月 7 日，没有写入运行账本）：

| 标的 | 旧逻辑 | 新逻辑 | 新请求响应总量 | 新逻辑耗时 |
| --- | --- | --- | --- | --- |
| sh510300 | 日期不完整，15,150 字节 | 增量成功，320 根，末根收盘价 4.634 | 5,752 字节，减少 62.0% | 1.33 秒 |
| sz159915 | 日期不完整，15,330 字节 | 增量成功，320 根，末根收盘价 3.414 | 5,784 字节，减少 62.3% | 2.18 秒 |
| sz159283 | 历史文件未含当天 | 收盘补齐接口 HTTP 502，保持未完成 | 不以失败请求计成功 | 3.10 秒 |

另验证 sh510300 的完整刷新返回前复权／未复权各 320 根，最早 2025-05-20、最新 2026-09-07。上述耗时是少量真实源读取，包含每主机每秒一次的限速等待，不能推算全市场完成时间；未放宽历史覆盖率、复权重叠校验或交易风控门槛。

扩大抽查发现 sh510500 历史末根含当天日期但 OHLC 和金额为空。修正占位行处理后，只读验证获得经收盘核验的 9 月 7 日数据：收盘价 7.771、实际成交额 5,158,719,200 元、成交份额 664,690,680。旧日空字段仍拒收，缺少有效收盘快照时当天也不计完成。

最终镜像已更新 dashboard 与 worker，健康检查正常，运行容器内适配器文件指纹与宿主一致，账户只读核对结果为空（无差异）。17:44:47 的运行状态为当日完成 **258/1211**，最近 5 分钟完成 **49** 只；部署前 17:37:48 为 199/1211、最近 5 分钟完成 2 只。该数据是部署前后观察窗口，不是固定吞吐量承诺；仍有 953 只待补充，当前唯一未就绪原因为历史覆盖率不足，后台继续自动采集与退避重试。

## 交易终端布局更新验收

当前界面改为深色交易终端风格，替代此前的浅色圆角主题。五页面共用顶部横向导航、紧凑面板、表格与状态栏；数字使用等宽字体，涨跌保留红涨绿跌。行情表格行高约 59px，表头固定，窄屏表格在内部滚动。筛选说明和历史同步详情按需展开；设置中的成交额单位仍为亿元，换手率仍为百分比。

使用独立临时数据库在 1440px、320px 检查总览、行情、策略、账户和设置页面，包含有持仓、有策略结果和空状态；均无整页横向溢出。验证分时与日 K 收盘线切换、分钟价格键盘查看、详情焦点循环、Escape 关闭并恢复焦点，以及通知区域和历史进度展开。真实本地关注池另在 1280px 检查，30 行行情正常显示，长名称及分时缩略图可读。

`./scripts/validate.sh` 通过：164 项测试、Ruff、Prettier、Vite 生产构建、Compose 配置与 diff 检查。仅重建 dashboard 容器，worker 持续运行；部署前后配置 v7、0 条成交和 1 条资金账本记录一致。全部界面测试数据只存在于临时数据库中。

## 管理密钥修改入口验收

设置页顶部增加“管理密钥”快捷入口及独立表单，包含当前密钥、新密钥和确认新密钥。新密钥要求 12–256 个字符，修改后所有旧会话失效，登录页显示使用新密钥重新登录的提示。凭据只保存加盐哈希，成功后清理初始化密钥文件；认证参数错误不会回显输入值。

新增 `tests/test_admin_password.py` 的 11 项测试，完整 **175 项测试在宿主与 Docker 中通过**。覆盖管理员会话与同源边界、旧密钥失效、所有会话撤销、错误当前密钥、失败次数持久化限流、短／超长／空白／相同／不一致输入与错误信息脱敏、重启后环境变量不恢复旧密钥、凭据／会话／审计事务回滚、初始化文件清理失败、过期会话、并发只成功一次、中文密钥，以及账户与配置保持一致。Ruff、Prettier、前端构建、Compose 配置和差异检查通过；隔离 Compose 的两日买入、持有、退出与重启核对通过。

使用临时数据库与独立 localhost 测试登录，在 1280px、390px、320px 检查入口、三项密码输入、长度限制及保存按钮，均无横向溢出，控制台无警告或错误。修正手机导航遮住快捷入口定位后的标题问题。实际改密及重新认证语义在隔离 API 测试中验证，未在正式账户提交新密钥。

## 设置分类精简验收

设置页拆为观察池、策略与风控、交易通知、管理密钥、数据与运行五个页签。默认只展示观察池；分类说明和成交费用按需展开，配置分类分别保存，运行记录每页 10 条。通知配置首次访问才加载，离开通知页签暂停历史轮询并保留草稿。

完整自动化套件 **175 项通过**，Ruff、Prettier、Vite 生产构建和 Compose 配置检查通过。使用隔离临时数据库实际操作：修改止损草稿后切换观察池保存成交额下限，服务端止损值保持不变，切回仍保留草稿；分别保存策略和调度后，其他分类数值保持不变。验证通知渠道草稿跨页签保留、未修改时禁用保存、运行记录翻页，以及折叠费用字段无效时自动展开并阻止提交。

1280 × 900 默认观察池内容在一屏内可见，390px、320px 五个分类均无整页横向溢出。管理密钥直达链接 `/settings#admin-security` 和键盘方向键、Home 切换通过，控制台无警告或错误。测试仅修改隔离数据库，未提交正式账户配置、管理密钥或外部通知。

## 明暗主题验收（2026-09-08）

右上角提供浅色／深色切换，登录前后均可用。浅色使用浅灰背景、白色圆角卡片，深色保留现有终端样式；表格、表头固定列、曲线、输入框、抽屉和状态提示使用对应主题变量。默认深色，浏览器保存选择，初始页面通过独立同源脚本提前应用主题，遵守现有 CSP。

完整套件 **175 项通过**，Ruff、JavaScript 语法检查、Prettier、Vite 构建及 Compose 配置检查通过。另使用隔离脚本核验已保存的浅／深色、无保存值、非法值和存储不可访问时的初始化行为。浏览器验证按钮点击与 Enter 切换、刷新恢复、同源多页面同步、浅色登录页、行情表格和账户净值曲线，控制台无警告或错误。

两套主题的五个页面均在 320px、390px 检查，无整页横向溢出；桌面 1280px 检查浅色总览、市场和登录页。补查 851px、900px、1024px 断点，修正新增按钮导致的 851px 导航溢出。所有界面操作使用临时数据库，主题选择仅影响测试浏览器，不修改正式交易配置或账本。

## 中文标题验收（2026-09-08）

浏览器标签、顶部品牌和登录页名称统一为“牛牛3号”，顶部采用牛头图标与中文单行标题。Prettier、Vite 生产构建及差异空白检查通过；隔离浏览器确认标签标题，检查浅色桌面、深色手机以及 851px、900px、390px、320px 布局，无横向溢出，控制台无警告或错误。本次仅修改标题文案和字号，未重复执行后端测试。


## 2026-09-09 数据源优先级

行情优先级调整为东方财富 → 腾讯财经 → 新浪财经 → 同花顺，按字段能力降级，覆盖 ETF 日 K、持仓盘口、ETF／指数分时和指数日 K。新增 13 项来源回退测试，连同既有数据、增量历史、换手率、分时和指数测试共 96 项通过；Ruff 与差异空白检查通过。验证包含部分批量报价补充、过期行情降级、缺少可执行盘口时继续下一源、来源恢复后不被旧缓存优先级覆盖、新浪未复权与同花顺前复权来源分开记录、上证指数不访问交易所身份不明确的同花顺接口。

小范围只读实测：煤炭 ETF（sh515220）截至 2026-09-08 的日 K 可经腾讯价格＋同花顺成交额返回；显式触发新浪备用价格分支也取得同日期的新浪未复权＋同花顺前复权；新浪 2026-09-09 分时返回 238 个实际分钟点，末点为 15:00，未补造缺失点。东方财富接口在本机仍会远端断开，源故障不等同于适配器测试成功；失败时保留缓存并按顺序降级。

全量 Python 测试中，原有 `test_credential_sessions_and_audit_roll_back_together` 仍失败：测试期待抛出 SQLite 异常，但现有 API 已统一返回 503。在恢复修改前行情模块的独立目录中也复现了同一失败，未将其归因于本次数据源改动。

## 2026-09-10 查看与变更权限

总览、ETF 名单与联想、策略信号及历史图表、模拟账户和交易记录无需登录即可查看；进入设置页面仍需验证管理密码。添加／移除 ETF、暂停／恢复交易、配置保存、通知测试和改密等变更接口保留管理员会话校验。前端在未验证时先弹出密码框，验证成功后继续原操作；取消或错误密码不执行变更，会话过期时重新验证后最多重试一次，服务错误不自动重试变更请求。

初始化、登录和改密均取消密码长度上限与字符组合限制，密码仍为必填；测试覆盖单字符、纯数字、中文、空格、首尾空格和 1024 字符密码，保留确认一致、当前密钥验证、限流、哈希和会话撤销机制。完整 Python **333 项**、前端 **88 项**测试通过，Ruff、Prettier、Vite 生产构建及差异空白检查通过。

使用独立临时数据库检查浏览器：未登录可查看总览、账户、ETF 和信号；设置入口展示密码验证；暂停操作先弹出密码框，取消与错误密码均不暂停，正确的单字符密码验证后才执行暂停。密码表单无 minlength／maxlength 属性。未修改正式账户、密码或运行状态。


## 2026-09-10 持续自动运行与顶部简化

页面顶部移除「模拟交易」标识及运行／暂停按钮，主题切换改为桌面 30px 高、窄屏 32px 方形。后台移除人工启动、暂停、恢复控制接口与交易引擎入口，状态仅表示运行、自动等待或后台离线；此前文档中的暂停／恢复验收为历史版本行为。

升级只清理旧暂停标记，保留账户、配置、名单、委托、成交、历史控制记录和风险退出意图，不恢复已取消委托。282 项 Python 测试和 94 项前端测试通过；覆盖已登录／未登录请求旧控制接口均返回 404 且不变更数据、迁移可重复执行、旧暂停标记不再阻断自动执行、休市后自动继续，以及行情和账本异常时仍阻止成交。桌面与 320px 窄屏检查主题尺寸、切换保存及顶部无手动控制按钮。

## 2026-09-10 移动端触控优化

常用按钮、展开入口和复选标签统一至少 44px 高；图表工具、副图切换和历史导航在窄屏分组排布。按钮、列表项和图表交互区域禁止长按选字及触控菜单，输入框与详情正文保留正常复制。日 K 与分时图均支持横向拖动查看，纵向滑动交给页面滚动，双指操作不进入回顾；拖动结束不额外打开买点标记。

前端 101 项测试通过，新增手势方向、点击、取消、多指操作、鼠标／键盘和菜单范围测试。Chromium 手机模拟覆盖 320、360、390、430px，另检查 768、1440px 布局；验证 700ms 长按后拖动、真实触摸事件滚动、输入后选择收起键盘，以及图表说明仍可复制。分时手势用浏览器内只读数据夹具验证，未更改正式账户和参数。本机 WebKit 与驱动协议不兼容，未完成 WebKit 或实体 iPhone 验证。

## 2026-09-11 移动端策略图表行内展开

移除手机策略页的「行情图表／ETF 列表」双视图，点按 ETF 在当前行下展开图表，再次点按收起；同一时间只加载一张，收起后卸载并停止图表轮询。图表复用列表中的名称与报价，保留分时、日 K 和图表设置。指定 ETF 链接定位到对应分类，不在分类中的标的和上证指数使用「当前查看」。桌面维持双栏。

前端 139 项测试、Prettier、Vite 构建、Compose 配置和差异检查通过。Chromium 手机模拟覆盖 320、390、430px，桌面覆盖 1440px；检查展开／收起、切换标的、分时／日 K、分类切换、指定 ETF 链接及窗口缩放，无重复图表、整页横向溢出或脚本错误。浏览器内独立快照另覆盖盘后候选、空信号、分类外标的和路由清除。WebKit 驱动与本机缓存版本不兼容，未完成 WebKit 或实体 iPhone 验证。本次仅更新 dashboard，保持 worker 运行。

## 2026-09-11 候选、入选与持仓栏目

策略页固定展示候选、入选、持仓三栏，移除独立买卖栏目。候选与入选按策略筛选分组；盘后合格参考放在入选，仍需次日盘中确认。持仓来自账户正持仓，与策略入选独立，可显示缺少策略行的实际持仓；列表显示持有及可卖份额，不将成本估值作为行情报价。分类外标的和指数临时置于当前列表顶部，不新增第四栏。

新增 4 项分组测试，前端共 143 项通过，覆盖退出信号仍有持仓、入选尚未成交、缺失信号、可卖为零、清仓及无效数量、盘后筛选和输入不变。Chromium 验证 320／390／430px 手机与 1440px 桌面，三栏数量与实时信号及账户一致，图表展开、收起、分时、深链接、指数和缩放通过。浏览器独立快照验证清仓刷新、缺失信号持仓、盘后分类与信号未就绪。Prettier、生产构建、Compose 配置和差异检查通过；仅更新 dashboard。
## 2026-09-12 取消额外模拟滑点

模拟撮合直接采用卖一价买入、买一价卖出，仅对不符合最小价位的报价按方向取整。普通订单、盘中调仓、分钟做 T 与日线情景回测均取消额外滑点；佣金、参与率、盘口检查与价格区间复核继续执行。设置页移除滑点字段，旧配置及待撮合订单兼容读取但忽略该字段，保存旧字段的请求原子拒绝，历史配置与成交不改写。

后端 345 项、前端 146 项测试通过，覆盖买卖盘口、取整、佣金与账本平衡、部分成交、旧配置重启及待撮合订单、API 字段移除和回测价格复核。Ruff、JavaScript 语法检查、Prettier、Vite 构建、Compose 配置及差异检查通过。本机 uv 因系统配置库异常退出，使用现有 `.venv` 直接执行相同 Python 检查。Chromium 本机与公网在 1440、390、320px 验证费用设置展开、滑点输入移除、佣金及参与率保留和无横向溢出；设置页使用浏览器内只读认证及配置夹具，未登录或保存正式参数。

重建更新 dashboard 和 worker，两容器健康，本机及公网 `/healthz` 返回 200，线上 JS/CSS 与本次构建的 SHA-256 一致。两个容器分别对银行 ETF 2026-09-09 的四条已保留报价只读计算，确认新买价等于卖一价、新卖价等于买一价；例如原成交 0.843 元对应卖一价 0.842 元，新规则计算为 0.842 元。未生成验证成交。部署前后正式成交、资金流水、持仓批次、原始配置、订单和名单哈希一致，账本核对无差异。
